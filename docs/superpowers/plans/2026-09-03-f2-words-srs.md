# F2 学外语科学性 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 配额分离（新词增量与复习补充分开）、7 档 SRS 曲线（当日强化 + 跨日）、难词休眠、例句中文翻译与词库全量补齐。

**Architecture:** 全部改动收敛在 `src-tauri/src/plugin/words.rs`（纯逻辑可测）+ `src/plugins/cards/word.ts`（渲染）+ `words-dict.json`（数据）。SRS 选词与记账是纯函数，先测后写；词库补齐是数据工程，按词书分批写入、`cargo test` 的词库完整性测试兜底。

**Tech Stack:** Rust（serde 状态迁移）、TypeScript、vitest / cargo test。

**Spec:** `docs/superpowers/specs/2026-09-03-auto-update-words-srs-panel-chrome-design.md` 设计二。

## Global Constraints

- 注释、文案、测试名全部中文（现有测试如 `认识推进梯度并封顶`）。
- 「不打扰是第一原则」：出卡节奏（15 分钟间隔 + only_resting 时间窗）本次**不改**。
- 词与释义永远来自内置词库；LLM 只生成例句/翻译/钩子。
- 旧状态文件必须无感迁移（`serde(default)` + alias），升级日学习进度不丢。
- 每个任务完成跑 `cd src-tauri && cargo test`（涉及前端再跑 `npx vitest run` 与 `npx tsc --noEmit`）。
- 版本号：合入时**问用户要**，不许自己编；三处真源同步改。

---

### Task 1: SRS 七档曲线 + 反馈规则 + 旧状态迁移（纯函数）

**Files:**
- Modify: `src-tauri/src/plugin/words.rs`
- Test: 同文件 `#[cfg(test)] mod tests`

**Interfaces:**
- Produces: `SRS_STEPS_MINS: [u64; 7]`、`DAY_CURSE_START: u32 = 3`、`LEECH_SLEEP_LAPSES: u32 = 5`、`fn on_feedback(e: &mut SrsEntry, known: bool, now: u64)`（签名不变语义变）、`fn migrate_step(old: u32) -> u32`、`fn migrate_state(s: &mut WordsState)`

- [ ] **Step 1: 改常量（words.rs 86-87 行附近替换）**

```rust
/// SRS 梯度（分钟）：当日强化 [10min, 30min, 2h] + 跨日 [1d, 3d, 7d, 21d]。
/// 没印象的词先在当天按强化曲线回来三次，再进跨日复习（多邻国式当日巩固）。
const SRS_STEPS_MINS: [u64; 7] = [10, 30, 120, 1440, 4320, 10080, 30240];
/// 当日强化区边界：step < 3 属当日，≥ 3 属跨日。
const DAY_CURVE_START: u32 = 3;
/// 连续这么多次「没印象」后该词休眠 7 天（防挫败循环，学 Anki leech）。
const LEECH_SLEEP_LAPSES: u32 = 5;
```

- [ ] **Step 2: 写失败测试（先加进 tests 模块）**

```rust
    // ---- 七档曲线（2026-09-03 修订） ----

    #[test]
    fn 梯度为当日强化加跨日七档() {
        assert_eq!(
            SRS_STEPS_MINS,
            [10, 30, 120, 1440, 4320, 10080, 30240]
        );
    }

    #[test]
    fn 首见即认识直接跳到跨日曲线_当天不再出现() {
        // 首见发卡 reps=1；点「认识」→ reps=2 且从未没印象 → step 直接到 3（明天见）
        let mut e = SrsEntry { due_mins: 0, step: 0, reps: 1, lapses: 0, first_mins: 0 };
        on_feedback(&mut e, true, 1000);
        assert_eq!((e.step, e.due_mins), (3, 1000 + SRS_STEPS_MINS[3]));
    }

    #[test]
    fn 复习中认识按档推进() {
        // 已在跨日区（step=3）的词点认识 → +1 档，不触发跳档
        let mut e = SrsEntry { due_mins: 0, step: 3, reps: 2, lapses: 0, first_mins: 0 };
        on_feedback(&mut e, true, 1000);
        assert_eq!(e.step, 4);
    }

    #[test]
    fn 当日强化区里没印象过的词认识只前进一档() {
        // step=0 但 lapses≥1（走强化曲线的词）：认识 → 30 分钟后重见，不跳跨日
        let mut e = SrsEntry { due_mins: 0, step: 0, reps: 2, lapses: 1, first_mins: 0 };
        on_feedback(&mut e, true, 1000);
        assert_eq!(e.step, 1);
        assert_eq!(e.due_mins, 1000 + SRS_STEPS_MINS[1]);
    }

    #[test]
    fn 没印象回强化曲线起点() {
        let mut e = SrsEntry { due_mins: 0, step: 4, reps: 3, lapses: 0, first_mins: 0 };
        on_feedback(&mut e, false, 1000);
        assert_eq!((e.step, e.due_mins, e.lapses), (0, 1000 + SRS_STEPS_MINS[0], 1));
    }

    #[test]
    fn 连续五次没印象休眠七天() {
        let mut e = SrsEntry { due_mins: 0, step: 2, reps: 5, lapses: 4, first_mins: 0 };
        on_feedback(&mut e, false, 1000);
        assert_eq!(e.lapses, 5);
        assert_eq!(e.due_mins, 1000 + 7 * DAY_MINS, "第 5 次没印象应休眠 7 天");
    }

    #[test]
    fn 旧五档状态等值迁移到新七档() {
        assert_eq!(migrate_step(0), 0, "旧 10min → 新 10min");
        assert_eq!(migrate_step(1), 3, "旧 1d → 新 1d");
        assert_eq!(migrate_step(2), 4, "旧 3d → 新 3d");
        assert_eq!(migrate_step(3), 5, "旧 7d → 新 7d");
        assert_eq!(migrate_step(4), 6, "旧 21d → 新 21d（封顶）");
        assert_eq!(migrate_step(9), 6, "越界封顶");
    }
```

同时**更新**旧测试以匹配新语义：

`认识推进梯度并封顶` 整体替换为（原版 `entry(0,0)` 起步会触发首见跳档，断言必挂）：

```rust
    #[test]
    fn 认识推进梯度并封顶() {
        // 复习中的词（已有反馈历史）认识才逐档推进：
        let mut e = entry(0, 3); // 跨日区 step=3
        e.reps = 3;
        on_feedback(&mut e, true, 1000);
        assert_eq!(e.step, 4);
        e.step = 6;
        on_feedback(&mut e, true, 1000);
        assert_eq!(e.step, 6, "最高档封顶");
    }
```

`没印象回起点十分钟后重见` **不用改**（`entry(0,3)` 的 lapses=0，走 `SRS_STEPS_MINS[0]` 分支，断言仍成立）；`梯度符合艾宾浩斯设计` 删除（已被 `梯度为当日强化加跨日七档` 取代）。

- [ ] **Step 3: 跑测试确认失败**

Run: `cd src-tauri && cargo test words`
Expected: FAIL（新测试编译错：`migrate_step` 未定义、断言不匹配）

- [ ] **Step 4: 实现 on_feedback 与迁移**

替换 `on_feedback`（原 235-245 行）：

```rust
/// 反馈（2026-09-03 七档曲线）：
/// - 首见即认识（reps 将变 2、从未没印象）→ 直接跳到跨日曲线 ——
///   认识的词当天不再出现，给新词让位，保证每日增量；
/// - 其余认识 → +1 档封顶；
/// - 没印象 → 回强化曲线起点、lapse+1；连续 LEECH_SLEEP_LAPSES 次 → 休眠 7 天。
fn on_feedback(e: &mut SrsEntry, known: bool, now: u64) {
    e.reps += 1;
    if known {
        let first_see_known = e.reps == 2 && e.lapses == 0;
        if e.step < DAY_CURVE_START && first_see_known {
            e.step = DAY_CURVE_START;
        } else {
            e.step = (e.step + 1).min(SRS_STEPS_MINS.len() as u32 - 1);
        }
        e.due_mins = now + SRS_STEPS_MINS[e.step as usize];
    } else {
        e.step = 0;
        e.lapses += 1;
        e.due_mins = if e.lapses >= LEECH_SLEEP_LAPSES {
            now + 7 * DAY_MINS
        } else {
            now + SRS_STEPS_MINS[0]
        };
    }
}

/// 旧 5 档 [10m,1d,3d,7d,21d] → 新 7 档按间隔时长等值映射：
/// 旧 0 → 0；旧 s≥1 → s+2（两档数值恰好一一对应）。
fn migrate_step(old: u32) -> u32 {
    if old == 0 {
        0
    } else {
        (old + 2).min(SRS_STEPS_MINS.len() as u32 - 1)
    }
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd src-tauri && cargo test words`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src-tauri/src/plugin/words.rs
git commit -m "feat(f2): SRS 七档曲线（当日强化+跨日）与首见认识跳档"
```

---

### Task 2: 配额分离（状态字段 + 迁移 + rollover）

**Files:**
- Modify: `src-tauri/src/plugin/words.rs`

**Interfaces:**
- Produces: `WordsState { served_new_count: u32, served_review_count: u32, schema: u32, ... }`、`fn rollover(s, today)`（语义扩展）、`fn migrate_state(s: &mut WordsState)`、`fn review_cap(daily_limit: u32) -> u32`

- [ ] **Step 1: 写失败测试**

```rust
    // ---- 配额分离 ----

    #[test]
    fn 旧状态served_count并入新词计数且迁移step与schema() {
        let mut s: WordsState = serde_json::from_str(
            r#"{"served_date":"2026-09-01","served_count":5,"served_terms":["a"],
                "srs":{"a":{"due_mins":10,"step":2,"reps":3,"lapses":0,"first_mins":0}}}"#,
        )
        .unwrap();
        migrate_state(&mut s);
        assert_eq!(s.served_new_count, 5, "旧 served_count 近似全是新词，并入新词计数");
        assert_eq!(s.served_review_count, 0);
        assert_eq!(s.srs["a"].step, 4, "旧 step 2(3d) → 新 step 4(3d)");
        assert_eq!(s.schema, 2);
    }

    #[test]
    fn 跨天清零两个计数() {
        let mut s = WordsState {
            served_date: "2026-09-01".into(),
            served_new_count: 5,
            served_review_count: 3,
            served_terms: vec!["a".into()],
            schema: 2,
            ..Default::default()
        };
        rollover(&mut s, "2026-09-02");
        assert_eq!((s.served_new_count, s.served_review_count), (0, 0));
        rollover(&mut s, "2026-09-02");
        assert_eq!((s.served_new_count, s.served_review_count), (0, 0), "同日不清");
    }

    #[test]
    fn 复习上限为每日限量的两倍() {
        assert_eq!(review_cap(8), 16);
    }
```

同时更新旧测试 `跨天清零当日计数`（引用了 `served_count` 字段，会编译失败）改用 `served_new_count` / `served_review_count`。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test words`
Expected: FAIL（`served_new_count` 等字段未定义）

- [ ] **Step 3: 实现状态改造**

`WordsState`（原 328-340 行）替换为：

```rust
#[derive(Debug, Default, Clone, Serialize, Deserialize)]
struct WordsState {
    srs: HashMap<String, SrsEntry>,
    /// 当日已发卡数（跨天清零）。
    served_date: String,
    /// 当日新词数（对 daily_limit）。旧字段 served_count 经 alias 并入。
    #[serde(default, alias = "served_count")]
    served_new_count: u32,
    /// 当日复习补充卡数（对 review_cap，不占新词配额）。
    #[serde(default)]
    served_review_count: u32,
    /// 当日已发词条（面板展示用，只留最近 20 条）。
    #[serde(default)]
    served_terms: Vec<String>,
    /// 上次发卡时刻（epoch 分钟），频率闸。
    #[serde(default)]
    last_card_mins: u64,
    /// 上一张卡的领域（打散用）。
    #[serde(default)]
    last_domain: String,
    /// 状态结构版本：1=旧（5 档 step、混合计数），2=当前。加载时据此迁移。
    #[serde(default)]
    schema: u32,
}
```

`rollover`（原 362-369 行）替换为：

```rust
/// 跨天清零（纯函数，单测入口）。新词与复习两个配额都清。
fn rollover(s: &mut WordsState, today: &str) {
    if s.served_date != today {
        s.served_date = today.to_string();
        s.served_new_count = 0;
        s.served_review_count = 0;
        s.served_terms.clear();
    }
}

/// 复习补充卡的每日上限：不占新词配额，但也不能无限刷屏。
fn review_cap(daily_limit: u32) -> u32 {
    daily_limit.saturating_mul(2)
}

/// 旧状态迁移（加载后调用一次）：step 等值换档 + 标记 schema=2。
fn migrate_state(s: &mut WordsState) {
    if s.schema >= 2 {
        return;
    }
    for e in s.srs.values_mut() {
        e.step = migrate_step(e.step);
    }
    s.schema = 2;
}
```

`load_state`（原 357-360 行）改为：

```rust
fn load_state(app: &tauri::AppHandle) {
    let mut s: WordsState = store::load(app, STATE_FILE);
    migrate_state(&mut s);
    with_state(|g| *g = s);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src-tauri && cargo test words`
Expected: PASS（tick 里对 `served_count` 的引用此刻会编译错——下一任务一并改）

- [ ] **Step 5: 提交**

```bash
git add src-tauri/src/plugin/words.rs
git commit -m "feat(f2): 学习状态配额分离与新旧 schema 迁移"
```

---

### Task 3: 选词五档优先级 + tick 记账

**Files:**
- Modify: `src-tauri/src/plugin/words.rs`（`pick`、`tick`）

**Interfaces:**
- Produces: `fn pick(pool, srs, now, user_rank, last_domain, allow_new: bool) -> Option<WordEntry>`（新增末参；`preview` 同步）、tick 内 `is_new` 记账逻辑

- [ ] **Step 1: 写失败测试**

```rust
    // ---- 五档选词优先级（2026-09-03） ----

    #[test]
    fn 当日强化到期词压过新词() {
        // a 是一小时前「没印象」的词（step<3 到期有反馈）；b 是新词 → 选 a
        let pool = vec![word("a", "life", "beginner"), word("b", "food", "beginner")];
        let mut srs = HashMap::new();
        srs.insert("a".into(), entry_today(9_940, 10_000, 60));
        let e = srs.get_mut("a").unwrap();
        e.reps = 2; // 有过「没印象」反馈
        let w = pick(&pool, &srs, 10_000, 0, "", true).unwrap();
        assert_eq!(w.term, "a");
    }

    #[test]
    fn 新词配额满时allow_new为false只发复习() {
        let pool = vec![word("a", "life", "beginner"), word("new1", "food", "beginner")];
        let mut srs = HashMap::new();
        let mut a = entry(9_940, 1);
        a.reps = 2; // 到期且有反馈（当日强化区）
        srs.insert("a".into(), a);
        let w = pick(&pool, &srs, 10_000, 0, "", false).unwrap();
        assert_eq!(w.term, "a", "新词被闸掉，复习照发");
        // 没有任何可复习词时，闸掉新词 = 直接不发卡
        let none = pick(&pool, &HashMap::new(), 10_000, 0, "", false);
        assert!(none.is_none());
    }

    #[test]
    fn 难词跨日到期压过普通跨日到期() {
        let pool = vec![word("hard", "life", "beginner"), word("norm", "food", "beginner")];
        let mut srs = HashMap::new();
        let mut hard = entry(0, 3); hard.reps = 4; hard.lapses = 2;
        let mut norm = entry(0, 3); norm.reps = 4; norm.lapses = 0;
        srs.insert("hard".into(), hard);
        srs.insert("norm".into(), norm);
        let w = pick(&pool, &srs, 10_000, 0, "", true).unwrap();
        assert_eq!(w.term, "hard");
    }

    #[test]
    fn 跨日已读词让位新词_语义保持() {
        // 既有回归不动：没人理过的词排队等新词（第五档兜底语义）
        let pool = vec![word("stale", "life", "beginner"), word("fresh", "food", "beginner")];
        let mut srs = HashMap::new();
        srs.insert("stale".into(), entry(0, 0));
        let w = pick(&pool, &srs, 10_000, 0, "", true).unwrap();
        assert_eq!(w.term, "fresh");
    }
```

并更新引用 `pick`/`preview` 的既有测试（`有反馈的到期复习词优先于新词`、`预览不重复…`、`领域打散…`、`领域无替代时不死锁`、`无到期词时选新词`、`水平过滤…`、`池子学完后才轮到当日重见`、`当日已见词让位新词_防同词霸屏`、`旧版遗留的锁死状态立刻转向新词`）：全部在 `pick(...)` 调用末尾补 `true`；`preview` 内部调用同步。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test words`
Expected: FAIL（arity 不匹配 + 新用例断言失败）

- [ ] **Step 3: 实现 pick 与 preview**

`pick`（原 257-317 行）替换为：

```rust
/// 选词优先级（2026-09-03 配额分离修订）：
/// 1. **当日强化到期**（step<3 到期且有反馈）—— 最近「没印象」的词最优先，
///    当天 10min/30min/2h 曲线上等它；
/// 2. **新词**（allow_new：新词配额未满才进这档）；
/// 3. **难词跨日到期**（lapses ≥ 2 且到期）；
/// 4. **普通跨日到期**（有反馈）；
/// 5. **当日已见 / 跨日已读兜底**（无反馈，现状语义）。
/// 水平过滤（允许挑战高一级）；领域打散（避开 last_domain，有替代才避开）。
fn pick(
    pool: &[WordEntry],
    srs: &HashMap<String, SrsEntry>,
    now: u64,
    user_rank: u8,
    last_domain: &str,
    allow_new: bool,
) -> Option<WordEntry> {
    let eligible: Vec<&WordEntry> = pool
        .iter()
        .filter(|w| level_rank(&w.level) <= user_rank + 1)
        .collect();

    let mut due: Vec<(&WordEntry, u64)> = eligible
        .iter()
        .filter_map(|w| {
            let e = srs.get(&w.term)?;
            (e.due_mins <= now).then_some((*w, e.due_mins))
        })
        .collect();
    due.sort_by_key(|(_, due_at)| *due_at);
    let has_feedback = |t: &str| srs[t].reps >= 2;
    let in_day_curve = |t: &str| srs[t].step < DAY_CURVE_START;
    let is_hard = |t: &str| srs[t].lapses >= 2;
    let is_old = |t: &str| now.saturating_sub(srs[t].first_mins) >= DAY_MINS;
    let from = |candidates: &[(&WordEntry, u64)]| {
        candidates
            .iter()
            .find(|(w, _)| w.domain != last_domain)
            .or_else(|| candidates.first())
            .map(|(w, _)| (*w).clone())
    };

    // 1. 当日强化到期（最近没印象的词）
    let reinforce: Vec<_> = due
        .iter()
        .copied()
        .filter(|(w, _)| has_feedback(&w.term) && in_day_curve(&w.term))
        .collect();
    if !reinforce.is_empty() {
        return from(&reinforce);
    }

    // 2. 新词（配额闸由调用方传入）
    if allow_new {
        if let Some(w) = eligible
            .iter()
            .find(|w| !srs.contains_key(&w.term) && w.domain != last_domain)
            .or_else(|| eligible.iter().find(|w| !srs.contains_key(&w.term)))
        {
            return Some((*w).clone());
        }
    }

    // 3. 难词跨日到期
    let hard: Vec<_> = due
        .iter()
        .copied()
        .filter(|(w, _)| has_feedback(&w.term) && is_hard(&w.term) && !in_day_curve(&w.term))
        .collect();
    if !hard.is_empty() {
        return from(&hard);
    }

    // 4. 普通跨日到期（有反馈）
    let review: Vec<_> = due
        .iter()
        .copied()
        .filter(|(w, _)| has_feedback(&w.term))
        .collect();
    if !review.is_empty() {
        return from(&review);
    }

    // 5. 当日已见到期 → 跨日已读，兜底
    let today_due: Vec<_> = due
        .iter()
        .copied()
        .filter(|(w, _)| !is_old(&w.term))
        .collect();
    if !today_due.is_empty() {
        return from(&today_due);
    }
    from(&due)
}
```

`preview`（原 49-78 行）的 `pick(&pool, &srs, now, user_rank, &last)` 调用补 `true`，函数其余不动。

- [ ] **Step 4: tick 记账改造**

`tick`（原 466-513 行）中 `with_state` 闭包里：

配额判断（原 `if s.served_count >= cfg.daily_limit { return None; }`）替换为：

```rust
            let allow_new = s.served_new_count < cfg.daily_limit;
            let allow_review = s.served_review_count < review_cap(cfg.daily_limit);
            if !allow_new && !allow_review {
                return None;
            }
```

`pick` 调用（原 484-490 行）改为传 `allow_new`：

```rust
            let word = pick(
                &pool,
                &s.srs,
                now,
                level_rank(&cfg.level),
                &s.last_domain,
                allow_new,
            )?;
```

记账（原 `s.served_count += 1;` 一行）替换为：

```rust
            // 配额分离：新词计入增量配额，一切重见（强化/复习/兜底）计入复习配额
            let is_new = !s.srs.contains_key(&word.term);
            if is_new {
                s.served_new_count += 1;
            } else {
                s.served_review_count += 1;
            }
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd src-tauri && cargo test`
Expected: PASS（全量，包括既有 words 之外的回归）

- [ ] **Step 6: 提交**

```bash
git add src-tauri/src/plugin/words.rs
git commit -m "feat(f2): 选词五档优先级与新词/复习配额分离记账"
```

---

### Task 4: meta 摘要与难词钩子置顶

**Files:**
- Modify: `src-tauri/src/plugin/words.rs`（`meta`、`make_card`）
- Modify: `src/plugins/cards/word.ts`（`renderSection`、`renderCard`）

**Interfaces:**
- Produces: summary JSON 新字段 `{ today_new: u32, today_review: u32, daily_limit }`（替代 `today_count`）；卡片 payload 新增 `"hard": bool`

- [ ] **Step 1: 写失败测试（Rust 侧）**

```rust
    #[test]
    fn 卡片payload难词带hard标记() {
        let w = word("test", "life", "beginner");
        let c = make_card(&w, None, true);
        assert_eq!(c.payload["hard"], true);
        let c2 = make_card(&w, None, false);
        assert_eq!(c2.payload["hard"], false);
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src-tauri && cargo test 卡片payload`
Expected: FAIL（`make_card` 只有 2 参）

- [ ] **Step 3: 实现 make_card 与 meta**

`make_card`（原 533-549 行）替换为：

```rust
fn make_card(w: &WordEntry, enhanced: Option<&Enhanced>, hard: bool) -> PluginCard {
    let example = enhanced.map(|e| e.example.as_str()).unwrap_or(w.example.as_str());
    let example_zh = enhanced
        .map(|e| e.example_zh.as_str())
        .filter(|s| !s.is_empty())
        .unwrap_or(w.example_zh.as_str());
    PluginCard {
        plugin_id: ID.into(),
        kind: ID.into(),
        priority: Priority::Normal,
        ttl_secs: 20,
        payload: serde_json::json!({
            "term": w.term,
            "reading": w.reading,
            "meaning": w.meaning,
            "example": example,
            "example_zh": example_zh,
            "hook": enhanced.and_then(|e| e.hook.clone()),
            "ai": enhanced.is_some(),
            "hard": hard,
        }),
    }
}
```

（`example_zh` 字段 Task 6 才加进 `WordEntry`/`Enhanced`——本任务先在两结构体加空实现：

```rust
// WordEntry 内（serde rename 风格与现有字段一致）：
    #[serde(rename = "ez", default)]
    pub example_zh: String,
// Enhanced 内：
    example_zh: String,
```

`spawn_enhance` 的 JSON 解析本任务先给 `example_zh: String::new()` 兜底，Task 6 完成接 LLM。）

tick 里 `vec![make_card(&word, enhanced.as_ref())]` 改为（`hard = lapses >= 3`）：

```rust
        let hard = with_state(|s| {
            s.srs.get(&word.term).is_some_and(|e| e.lapses >= 3)
        });
        vec![make_card(&word, enhanced.as_ref(), hard)]
```

`meta`（原 553-612 行）中：`let (count, terms, ...)` 元组与 summary 的 `today_count` 改为双计数 —— 把 `(s.served_count, ...)` 改 `(s.served_new_count, s.served_review_count, ...)`，summary JSON 里 `"today_count": count` 替换为：

```rust
            "today_new": new_count,
            "today_review": review_count,
```

（`remaining` 计算用 `cfg.daily_limit.saturating_sub(new_count)`。）

- [ ] **Step 4: 前端 renderSection 适配（word.ts 106-124 行）**

```ts
  renderSection(data) {
    const s = data as {
      enabled: boolean;
      language: string;
      today_new: number;
      today_review: number;
      daily_limit: number;
      learned: { term: string; meaning: string }[];
      upcoming: { term: string; meaning: string }[];
    };
    const el = document.createElement("div");
    el.className = "pet-card-word-section";
    if (!s.enabled) {
      el.textContent = "未启用";
      return el;
    }
    const head = document.createElement("div");
    head.className = "pet-word-section-head";
    head.textContent = `今日新学 ${s.today_new}/${s.daily_limit} · 复习 ${s.today_review}`;
    el.appendChild(head);
```

（其余 renderList 部分不动。）

`renderCard` 的 `WordPayload` 接口加 `example_zh: string; hard: boolean;`；钩子渲染（原 73-78 行）改为难词置顶：

```ts
    // 难词（连续没印象）钩子置顶：紧跟词条，先于释义 —— 越难越要先给记忆抓手
    const hookEl = () => {
      const hook = document.createElement("div");
      hook.className = "pet-word-hook";
      hook.textContent = `💡 ${p.hook}`;
      return hook;
    };
    if (p.hook && p.hard) el.appendChild(hookEl());
```

例句翻译渲染（原 81-86 行例句块之后追加）：

```ts
    if (p.example) {
      const example = document.createElement("div");
      example.className = "pet-word-example";
      example.textContent = p.example;
      el.appendChild(example);
      if (p.example_zh) {
        const zh = document.createElement("div");
        zh.className = "pet-word-example-zh";
        zh.textContent = p.example_zh;
        el.appendChild(zh);
      }
    }
```

（非难词的钩子保持原位：`if (p.hook && !p.hard) el.appendChild(hookEl());` 放在 meaning 之后。）

- [ ] **Step 5: 验证**

Run: `cd src-tauri && cargo test && cd .. && npx tsc --noEmit && npx vitest run`
Expected: 全绿

- [ ] **Step 6: 提交**

```bash
git add src-tauri/src/plugin/words.rs src/plugins/cards/word.ts
git commit -m "feat(f2): 面板新学/复习分列展示，难词钩子置顶，翻译行渲染"
```

---

### Task 5: LLM 增强 prompt 扩展（example_zh）

**Files:**
- Modify: `src-tauri/src/plugin/words.rs`（`spawn_enhance`）

**Interfaces:**
- Consumes: Task 4 的 `Enhanced.example_zh`
- Produces: LLM 增强缓存条目含中文翻译

- [ ] **Step 1: 实现 prompt 与解析**

`spawn_enhance` 中 system prompt（原 402-407 行）替换为：

```rust
        let system = concat!(
            "你为外语单词生成学习卡增强内容，只输出 JSON，不要任何其他文字：",
            "{\"example\":\"一句使用该词的例句（与用户目标场景贴合）\",",
            "\"example_zh\":\"该例句的中文翻译（自然、不逐字直译）\",",
            "\"hook\":\"一个中文记忆钩子：词根拆解、谐音或联想，三选一，25字内\"}。",
            "例句语言：英语单词用英文句子，日语单词用日文句子。例句要自然、略高于课本感。"
        );
```

解析段（原 419-425 行）替换为：

```rust
        let enhanced = Enhanced {
            example: v["example"].as_str().unwrap_or_default().to_string(),
            example_zh: v["example_zh"].as_str().unwrap_or_default().to_string(),
            hook: v["hook"].as_str().map(str::to_string),
        };
        if enhanced.example.is_empty() {
            return;
        }
```

- [ ] **Step 2: 验证并提交**

Run: `cd src-tauri && cargo test`
Expected: PASS

```bash
git add src-tauri/src/plugin/words.rs
git commit -m "feat(f2): LLM 例句增强附带中文翻译"
```

---

### Task 6: 词库全量补齐（例句 + 翻译，数据工程）

**Files:**
- Modify: `src-tauri/src/plugin/words-dict.json`
- Modify: `src-tauri/src/plugin/words.rs`（词库完整性测试强化）

**Interfaces:**
- Consumes: Task 4 的 `ez` 字段
- Produces: 2255 条词条 `e`（例句）与 `ez`（中文翻译）全部非空

- [ ] **Step 1: 强化词库测试（先红）**

`词库可解析且词条完整` 测试中，替换例句断言（原 880-886 行）为：

```rust
                    // 2026-09-03 词库补齐后基线抬升：例句与翻译全量必填，
                    // ECDICT 扩展词书（*_x）不再豁免
                    assert!(
                        !w.example.is_empty(),
                        "例句为空：{}（{lang}/{id}）",
                        w.term
                    );
                    assert!(
                        !w.example_zh.is_empty(),
                        "例句翻译为空：{}（{lang}/{id}）",
                        w.term
                    );
```

Run: `cd src-tauri && cargo test 词库可解析`
Expected: FAIL（约 1300 条 `e` 为空、全部 `ez` 为空）

- [ ] **Step 2: 盘点词书清单**

```bash
python3 -c "
import json
d = json.load(open('src-tauri/src/plugin/words-dict.json'))
for lang, books in d.items():
    for bid, b in books.items():
        no_e = sum(1 for w in b['words'] if not w.get('e'))
        print(f\"{lang}/{bid}: {len(b['words'])} 词，{no_e} 条缺例句\")
"
```

记下每本词书的两个数：需补 `e` 的（ECDICT `*_x` 全部）、需补 `ez` 的（全部）。

- [ ] **Step 3: 分批写入（约 8-10 批，每批一本或半本词书）**

对每批：直接编辑 `words-dict.json`，给目标词条补齐 `"e"` 与 `"ez"`。**内容质量标准（硬性）**：

- 英语例句：自然口语/书面句，6–14 词，使用该词最常见义项；不出俚语、不出古典用法。
- 日语例句：自然，≤22 字，です/ます体（beginner 词书），普通名词句可用简体断言句。
- `ez` 中文翻译：忠实且自然的中文，≤30 字，禁止逐字翻译腔；译主语按语境补全。
- 难度匹配 `l` 档位：beginner 的例句用初阶词汇，advanced 可用复合结构。

每批完成后跑：

```bash
cd src-tauri && cargo test 词库可解析
```

直到该批涉及的词条不再出现在 FAIL 列表。**逐批提交**：

```bash
git add src-tauri/src/plugin/words-dict.json
git commit -m "data(f2): 词库补齐 <词书名> 例句与中文翻译（N 条）"
```

- [ ] **Step 4: 全量绿**

Run: `cd src-tauri && cargo test`
Expected: PASS（含 `词库各语言总量与领域多样性达标` 等全量回归）

---

### Task 7: 翻译行样式 + 设置提示文案 + 版本合入

**Files:**
- Modify: `index.html`（`.pet-word-example` 样式块之后加翻译行样式）
- Modify: `src/plugins/cards/word.ts`（renderSettings 提示文案，原 217-221 行）
- Modify: `src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`package.json`（版本三处）

- [ ] **Step 1: 翻译行样式**

index.html 中 `.pet-word-example` 规则后插入：

```css
      .pet-word-example-zh {
        margin-top: 2px;
        color: #8b93a7;
        font-size: 11px;
        line-height: 1.5;
      }
```

- [ ] **Step 2: 设置提示文案更新（word.ts renderSettings 的 hint）**

```ts
    hint.textContent =
      "开启后第一张立即出现；之后在键盘静默 1 分钟后才弹。每日上限只数新词，复习卡是额外补充。「没印象」的词当天会按 10 分钟/半小时/2 小时回来，隔天再复习；认识的词当天不再出现，给新词让位。词、释义、例句与翻译都来自内置词库；配置 AI 后例句与记忆钩子按你的目标定制";
```

- [ ] **Step 3: 手工验证（`pnpm tauri dev`，学外语开启）**

- [ ] 词卡例句下方出现灰色中文翻译行
- [ ] 点「没印象」的词在 ~10 分钟后重新出现（强化曲线第 1 档）
- [ ] 点「认识」的新词当天不再出现，新词继续来（增量保证）
- [ ] 左键面板显示「今日新学 N/上限 · 复习 M」
- [ ] ECDICT 扩展词（无例句的 `_x` 词书）出卡时带例句与翻译

- [ ] **Step 4: 全量检查**

Run: `npx tsc --noEmit && npx vitest run && cd src-tauri && cargo test`
Expected: 全绿

- [ ] **Step 5: 版本号（问用户，不许自己编）并合入**

三处 `version` 同步为用户给的版本号，然后：

```bash
git add -A
git commit -m "feat: 学外语配额分离与科学复习曲线

版本: <用户给的版本号>

- SRS 七档曲线：当日强化 10min/30min/2h + 跨日 1d/3d/7d/21d；
  首见即认识直接跳跨日，当天给新词让位（保证每日增量）
- 配额分离：daily_limit 只数新词，复习卡独立上限 2× 不占增量；
  选词五档优先级（当日强化 > 新词 > 难词 > 普通复习 > 兜底）
- 难词处理：lapse≥3 钩子置顶；连续 5 次没印象休眠 7 天
- 词库补齐：2255 条例句全部带上中文翻译（ez），ECDICT 扩展词
  补例句；词库完整性测试基线抬升（e/ez 全量必填）
- LLM 增强例句同步带翻译；旧学习状态无感迁移

验证：cargo test N 绿 / vitest N 绿 / tsc 通过 / 手工清单过"
```
