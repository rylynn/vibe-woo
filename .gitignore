node_modules/
.codebuddy/
.gitignore
docs/
dist/
src-tauri/target/
.DS_Store
*.local
# 仅规则目录进版本库：合入规范（比如版本号要求）要跟着代码一起走，团队共用
.codebuddy/*
!.codebuddy/rules/

# 环境与部署产物（含项目标识，不进版本库）
.env
.env.*
!.env.example
.edgeone/

# 服务端本地数据（本地联调产生的账号数据，不进版本库）
worker-edgeone/.local-data.json
worker/.dev.vars

